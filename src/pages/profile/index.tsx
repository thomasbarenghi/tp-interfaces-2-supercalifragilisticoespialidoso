import { usePageTitle } from '../../hooks/usePageTitle.ts'

const Profile = () => {
    usePageTitle('Profile')
    return (
        <>
            Profile
        </>
    )
}

export default Profile
