import { usePageTitle } from '../../hooks/usePageTitle.ts'

const EditUser = () => {
    usePageTitle('EditUser')
    return (
        <>
            EditUser
        </>
    )
}

export default EditUser
