import { usePageTitle } from '../../hooks/usePageTitle.ts'

const EditPost = () => {
    usePageTitle('EditPost')
    return (
        <>
            EditPost
        </>
    )
}

export default EditPost
