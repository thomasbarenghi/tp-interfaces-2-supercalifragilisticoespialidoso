import { usePageTitle } from '../../hooks/usePageTitle.ts'

const Register = () => {
    usePageTitle('Register')
    return (
        <>
            Register
        </>
    )
}

export default Register
