import { usePageTitle } from '../../hooks/usePageTitle.ts'

const PostDetail = () => {
    usePageTitle('PostDetail')
    return (
        <>
            PostDetail
        </>
    )
}

export default PostDetail
