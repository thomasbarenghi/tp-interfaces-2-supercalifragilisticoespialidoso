import { usePageTitle } from '../../hooks/usePageTitle.ts'

const CreatePost = () => {
    usePageTitle('CreatePost')
    return (
        <>
            CreatePost
        </>
    )
}

export default CreatePost
